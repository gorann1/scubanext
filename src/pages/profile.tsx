import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Profile = () => (
  <Main meta={<Meta title="Profile Page" description="Profile Page" />}>
    <h2 className="text-2xl font-bold">Profile Page</h2>
  </Main>
);

export default Profile;
